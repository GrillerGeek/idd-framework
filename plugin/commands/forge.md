---
description: Launch the Forge web UI to browse and edit IDD artifacts in this repo
argument-hint: "[--port <number>] [--no-open] [--docs <path>]"
allowed-tools: "Bash(npx *) BashOutput"
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/idd-forge/SKILL.md` and follow its complete launcher procedure for `$ARGUMENTS`. Preserve the arguments as literal tokens; do not pass raw shell source through. Use the actual Claude background Bash capability and its returned output/task handle, with bounded startup inspection and owned stop instructions. If available permissions cannot read the procedure or inspect/stop the launched process, report that capability limitation rather than inventing success.
