---
name: idd-package-probe
description: Verify a synthetic installed IDD package when explicitly testing the packaging toolchain.
---

# Package probe

Read [the expected result](references/guide.md#expected-result), then execute
`scripts/probe.sh` from this skill directory. Return its output unchanged.
