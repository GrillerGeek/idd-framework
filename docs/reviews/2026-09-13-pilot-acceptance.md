# Portable pilot acceptance disposition

Date: 2026-09-13. Orchestration decision under the user's explicit unattended migration authorization: the three-stage pilot may advance to review and remaining-stage implementation may begin using the measured invocation lanes below. Actual human peer review remains pending. This is not release/native-plugin certification.

The historical SPEC-b2e3 host report remains unchanged: it correctly records the earlier Claude implementation failures. SPEC-c45b supplies a separately reviewed installed terminal controller, explicit reviewed execution metadata, and successful independently reviewed configured-model evidence. This closes the particular execution-checkpoint blocker without accepting tool echoes as acknowledgments or weakening lifecycle/report requirements.

| Workflow | Codex evidence | Claude evidence and supported lane |
|---|---|---|
| Interview | Previously accepted installed-copy scenario | Previously accepted installed-copy scenario |
| Gap-check | Previously accepted clean/adversarial/incomplete and report-only scenarios | Previously accepted adversarial/incomplete cases; clean case accepted with process-local Default output style. Custom-style clean acceptance remains unclaimed. |
| Implementation | Previously accepted installed-copy build with actual acknowledgments and refusal | SPEC-c45b configured-policy installed terminal run KwgjFe accepted by independent trace review; warnings gate refuses with zero model calls. Requires reviewed execution_contract. |

Sonnet model selection works but full workflow trace acceptance failed. Ordinary/native Claude dispatch still lacks certification; it does not gain controller guarantees automatically. These limitations stay visible during broader implementation and remain release follow-ups. Existing per-host aliases, client UX, complete-catalog distribution and hosted CI still need their later integration evidence.

The pilot exit criterion is interpreted as successful installed workflow operation through the documented supported lanes, not universal success under every optional configuration. This matches the plan's separate later native/alias integration gate. No old failed run, human fact or historical report has been rewritten to manufacture acceptance.

References: SPEC-b2e3-host-evaluation.md (historical matrix), SPEC-c45b-host-evaluation.md (new installed execution and trace evidence), SPEC-c45b-gap-check.md (new runtime contract review).
