# Exploration host evaluation — SPEC-c11a

Fourteen stages now have standalone bundles. Exploration adds chart and resolve with four metadata-preserving Claude adapters. All twelve required cases now pass separate output revalidation and independent full-trace review. Output-oracle success alone does not establish procedural acceptance.

## Verification

335 full tests passed on Node 25.8.1 and minimum 22.20.0, followed by 53 targeted tests on both after two additional regressions (337 final total). All 60 individual catalog/probe installation combinations pass across both hosts and copy/symlink modes. Copy-source removal is real. These are individual installations, not a bulk installation. Assembly produces 156 deterministic files; adapter frontmatters and protected resources remain intact.

## Accepted observations

Evidence directories are under `/var/folders/f0/_sscvrpd48s9srngb0d0l_mh0000gn/T/`, named `idd-exploration-<host>-<scenario>-<receipt>`. Each retains baseline.json, result.json, prompt.txt, stdout.log and stderr.log. Receipts have not been rewritten. Models use the configured host policy, existing authenticated CLI and an independently installed copied skill with source removed. Each observation is bounded to 600 seconds and 4 MiB. Local fixture facts and sequential research are explicit; no external research or personal settings changes occur.

| Case | Claude | Codex | Independently reviewed behavior |
|---|---|---|---|
| Confirmed chart | r38fH9 | O6hV7J | Actual unpublished local research, no fabricated shared claim, complete proposal batch; unanswered grilling/fog retained; exact original plus intended owned state guards around every mkdir/write. |
| No fog | MDiPqF | ZlR5rH | No writes, complete preservation, truthful handoff. |
| Selected task | EsGmB7 | G6jHe8 | Real selected-path claim before FACTS.md; unrelated staged and further dirty note retained; one non-research ticket only. |
| Human input | wpTaXy | hqMjPf | Verified selected claim before one unanswered question; no fabricated answer or map resolution. |
| Blocked cycle | 73nAod | vFecCD | Map then ticket headers; named dependency cycle refused, no claim/research/writes, no false clear despite empty fog. |
| Missing research | oQL8pV | KSJ9W5 | Actual verified claim precedes first source-specific probe; missing MISSING.md reported, claim remains unresolved and no decision gist. |

Claude task claim: 47807bf; Codex task: 4798c61; Codex human input: 0cd57bc; Claude research retry: 59fe06f; Codex research: d87e058. All are disposable fixture commits, never commits to this project. Codex G6jHe8 corrected an initial inventory attempt before any content parsing; the first actual full baseline was retained. KSJ9W5 checks all descendant inventory membership/types/modes/hashes and root/ancestor directory types. The independent fixture additionally verifies unchanged root mode and Git configuration; the model itself did not capture those two hashes/modes. Do not conflate independent verification with model-executed checks.

## Original failures and reinforced retries

- Claude task qYlfCa produced valid output but omitted complete unowned directory/mode/membership checks. EsGmB7 is a fresh accepted retry.
- Claude chart 1St19C produced valid output but omitted directory modes and only allowed previously owned path names without checking exact intended content between writes. r38fH9 is a fresh accepted retry.
- Claude missing research P0wQBZ probed MISSING.md before the actual claim and omitted complete preservation guards. oQL8pV is a fresh accepted retry.
- Claude human-input jTL3eF passes the corrected output oracle but fails full procedure: files-only guards omit directory state/membership, and frontier inspection emits the unselected ticket's full bytes. Its claim 51c7643 and missing answer remain real, but are insufficient. A fresh retry additionally reinforces frontmatter-only working context and complete inventory checks.

Retries reinforce the written procedure in fixture prompts. Their acceptance is bounded reinforced-prompt evidence, not ordinary-prompt reliability certification. Required capability failures remain failed/blocked; expected missing sources and unavailable optional workers are distinguished from required parser/Git failures.

Claude retry AwgHKj fixed the full-inventory and frontier-context issues and verified real claim cae2b48, but asked two interrogative sentences. It remains failed for the one-question requirement; fresh retry wpTaXy specifies the exact single question and passes independent full-trace review. No acceptance oracle was relaxed.

## Corrections and provenance

Independent oracle review added exact unowned frontmatter/outside-byte protection, invalid dependency graph rejection even when stored tickets are resolved, safe quoted ownership/CRLF support, and a question-count correction for self-answered rhetorical headings. Original receipts remain unchanged and final revalidation is separate.

Earlier long-lived matrix processes imported helper modules before later source edits; recorded per-case disk hashes could describe newer files than the loaded module. Actual copied bundle hashes, prompts and complete original traces remain evidence. Final revalidation records the current oracle separately. The current evaluator captures source hashes at module load; no retroactive exact-module claim is made for older cases.

Final source clarifies that a later human answer continues the ongoing conversation with its existing verified claim and a fresh expected-state check; changed/lost state needs explicit recovery. Both canonical and legacy format references explicitly exclude missing dependencies/cycles from clear. These are narrow source clarifications after some observations; later-answer continuation itself has not been tested.

Final separate revalidation: 12 cases passed with oracle SHA-256 `60f124b8fcc94871635e66878def55b2a2081dc14852e2ead9ccaecd631e4274`; detailed revalidation is retained at `/tmp/idd-exploration-accepted-revalidation.json`.

## Limits

Actual human peer review remains pending under the recorded unattended migration authorization. Native aliases, cross-session claim races, atomic/crash recovery, all external research/prototype tools and later-answer continuation are not certified. Configured models and observed output styles are recorded per receipt; no blanket model compatibility claim. No public installation, actual-project exploration mutation, push, tag or publication occurred.
