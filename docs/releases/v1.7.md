# Framework 1.7 / plugin 1.7.0 — unreleased local candidate

Prepared on `codex/portable-skills`: version-only candidate `bb2f9e8`, evaluation
recovery `7576935`, Archive closure `2970f3d`, router closure `68836de` and
native closure `2fe5180`. **Local implementation and its required automated/host
acceptance are complete; actual human review remains pending.** All four final
Specs are in `review`, not `done`. No repository push, release tag, public release,
personal installation or external marketplace update has occurred.

## What changed

- Shared contributor guidance, repository checks and deterministic packaging make
  the project usable from Codex while preserving IDD artifact schemas and gates.
- Sixteen portable skills provide fifteen complete workflow stages and a router
  containing all stage resources. Consumers can select individual skills or the
  router through pinned `npx skills@1.5.25`; no consumer npm dependency is added.
- The installed Claude execution controller checks readiness and explicit
  writable outputs, obtains actual read-only acknowledgments before writes,
  verifies the execution report and keeps lifecycle transitions in orchestration.
- Portable authoring, review, controlled Forge and Exploration stages have actual
  Codex/Claude observations. All eight required Archive observations are
  independently accepted, including complete
  Claude apply. The measured Claude lane uses configured model/style and explicit
  low effort; prior failed and output-only traces remain documented.
- Native Codex and Claude manifests and repository-local catalogs support
  installation, refresh and removal. Claude retains its fifteen commands,
  fourteen agents and two configuration fields. Both native lifecycle probes
  preserve unrelated plugins, settings and consumer files in isolated fixtures.
- Current production manifests and their strict reviewed version assertions move
  from 1.6.0 to 1.7.0. This final preparation changes no workflow, generated
  resource, runtime, adapter body/frontmatter, dependency or historical report.

## Installation and compatibility

Follow the [installation guide](../installation.md) for built local checkouts.
Choose native installation, the standalone router or selected standalone stages;
avoid duplicate routes within one client. GitHub shorthand and published-ref
installation remain unverified for this migration.

The reviewed standalone installer is pinned at 1.5.25. Single-host installation
copies even without `--copy`; dual-host installation can link Claude into the
project's canonical skills directory. Refresh a local source by repeating the
explicit add command. Host-scoped removal can retain shared canonical copies;
those copies remain installed. Do not broaden deletion to unrelated hosts.

Native local Codex refresh uses a changed version and re-add; its marketplace
upgrade command is Git-only. Claude supports its named local marketplace update.
Codex removes the observed installed caches, while Claude unregisters the plugin
and retains orphaned cached versions. These behaviors were observed with Codex
0.153.4 and Claude Code 2.1.269; they do not certify every future client version.

Contributors and the guarded terminal runner require Node 22.20.0 or newer.
Helpers require Bash, and YAML-using workflows need a safe parser. The Claude
terminal runner requires existing authentication and preserves nested-session
guards. The configured Claude lane is accepted for its measured trace; optional
Sonnet model selection works but its full-procedure lane remains experimental.
Native alias execution, fresh-session desktop discovery, public Forge UI behavior,
real concurrent Exploration writes and later-answer continuation remain unverified.

## Evidence and limits

The [comprehensive catch-up report](../reviews/2026-09-13-overnight-catch-up.md)
links every milestone and records final candidate verification separately from
historical runs. The [final release execution report](../reviews/SPEC-3671-20260915T014756Z-execution.md)
records exact version and preservation checks.

Actual [router observations](../reviews/SPEC-ab84-host-evaluation.md) and
[native lifecycle observations](../reviews/SPEC-aa60-native-evaluation.md) have
independent acceptance. The final
[Archive case](../reviews/SPEC-57b4-host-evaluation.md), QqBauk, also
passes independent full-trace review: original filesystem/Git binding, guards
around every mutation, persisted reconciliation, exact staging/commit and complete
recovery. Earlier sleeping-host, partial and output-only failures remain preserved.
The original source-checkpoint reports are unchanged; new closure reports record
final acceptance. Human implementation review remains pending.

Both final uninterrupted full suites pass **499/499** on Node 25.8.1 and minimum
22.20.0. Package checks, exact 318-file assembly, metadata/preservation audits and
68 final-candidate individual installer combinations per runtime pass. Four bulk
standalone and two native lifecycle observations are separate evidence. See the
[final acceptance record](../reviews/2026-09-15-final-acceptance.md) for log hashes
and the preserved earlier failures. No hosted CI or remote installation success is
inferred from local testing.

## Remaining release sequence

1. Complete maintainer review of installation choices, recorded limitations and
   the candidate. Unattended authorization is not completed human peer review;
   follow the remaining validation lifecycle before marking artifacts done.
2. After publication authorization, push/open the PR and inspect actual hosted CI.
   Verify installation from the intended published ref/default branch in isolated
   consumers. Local-path evidence does not prove GitHub shorthand behavior.
3. After merge/release authorization and successful checks, perform the release
   tag and publication steps. Coordinate the separately owned external
   `GrillerGeek/skills` marketplace rather than editing it as part of local work.

Personal installation remains a separate user choice. These steps describe work
still to do; none is a claimed publication event.
