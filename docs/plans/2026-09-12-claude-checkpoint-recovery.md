# Claude execution checkpoint recovery contract

The user requested the Claude execution checkpoint after accepting the migration progress report. Earlier authorization permits choosing implementation details and documenting decisions. This is a focused recovery of SPEC-b2e3, not permission to waive visible acknowledgments or certify native installation.

Implement an opt-in staged Claude evaluation controller. Keep direct evaluations unchanged. Use one fresh, controller-owned Claude session in a disposable fixture, resumed across three invocations: read-only orchestration acknowledgment; read-only implementation acknowledgment; writable implementation. The controller validates readiness and report evidence before launching, validates each actual assistant acknowledgment and unchanged project snapshot before continuing, and alone changes lifecycle. The same session preserves the implementer's own acknowledgment. No extra human approval is needed between stages. Both read-only invocations expose only Read, Glob and Grep; no shell, Skill expansion, delegation, MCP or hooks. The writable invocation uses the existing bounded tool set without delegation. Preserve configured model/output style, existing authentication and permission mode. The explicit staged mode necessarily enables normal Claude session persistence to resume this one session; report this difference, do not edit personal settings or installations, and do not delete unrelated host runtime data.

Total model time remains <=600 seconds, separate output/test verification <=10 seconds each, total output <=4MB. Stop before the next phase on missing/invalid/late acknowledgment, host error, changed fixture or gate evidence. Controller status edits preserve every other Spec byte. It advances in-progress to review only after actual implementation outputs, report and preservation checks pass. Treat a model-written lifecycle change as failure rather than silently repairing it. A failed started build stays in-progress when the model obeys lifecycle ownership; unexpected model writes are evidence of failure and are preserved/reported. This is workflow coordination, not an OS sandbox against hostile generated code.

Own scripts/evaluate-pilot.mjs, new scripts/lib/claude-checkpoint.mjs, tests/helpers/pilot.mjs, new tests/checkpoint.test.mjs and necessary existing pilot tests; canonical implementation workflow/role instructions and generated copies; contributor/pilot documentation and new review reports. Do not modify the other workflows, legacy router, command metadata/model assignments, manifest/version/hooks, historical reports or existing Spec contract blocks. Track acceptance separately; no claims that the ordinary one-shot Claude invocation or native plugin has been fixed by controller-only evidence.

Tests: missing/partial/wrong-role acknowledgment blocks continuation; echoed/private/report text does not count; read-only mutation blocks continuation; parser/gate failures start no host; failures/timeouts block later phases; controller preserves exact Spec content and pre-existing dirty files; configured model/style and disabled hooks/MCP survive resume; one shared wall-clock/output budget; model lifecycle write fails; successful three-phase run advances only after verification. Run fresh actual Claude clean and refusal cases in staged mode, then an ordinary Codex regression after shared prose changes. Preserve old failures and record provenance/settings. Independent review the implementation and actual trace; no inferred human peer approval.

Author resolutions after independent review: verifyScenario gets explicit expectedStatus='in-progress' for the controller's pre-advance checks, retaining default review for direct runs. No temporary review transition. Add strict report validation in staged mode: every required exact row has pass and nonempty evidence; reject provisional/failed/unrun rows and any unresolved blocker-grade gap. Human-only follow-ups remain permitted. Controller safely parses duplicate-rejecting YAML and validates gate/recorded fixture readiness before host work, supplies that evidence for the read-only role's inspection, and rechecks exact files before transitions. Read-only phases never need shell parsing. All init/result session IDs must equal the controller-created UUID across resume; missing/conflicting IDs block continuation. Invalid staged gates produce a controller-refusal result with zero workflow host invocations, explicitly distinct from Claude refusing a gate. Verification tests and documentation preserve that distinction.

Observed resume correction: a bounded real capability probe retained its session UUID and ELI5 style but changed the reported model from claude-opus-5[1m] to claude-opus-5 on resume. The controller must retain the exact first-init model string and pass that same observed identity with --model on resumes, then verify the actual init identity matches. This preserves the configured selection; it is not a chosen model-tier override. Evidence records each phase and the restoration source. No model flag is added to the initial invocation or direct evaluator. Verify exposed tools against the phase allowlist in actual init evidence and reject unexpected tool calls, including calls claimed unavailable by the intended flags. Require implementation paraphrases as indexed boundary blocks with a nonempty separate explanation line; semantic adequacy remains trace review.
